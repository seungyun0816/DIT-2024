Python문자열의 특징

  구간인덱싱

  nagative인덱싱
  [-]를 이용해 문자열의 뒤쪽 인덱스 값 부터 가져오는 것

  st.upper(),st.lower()
  각각 문자열을 대문자,소문자로 바꾸어서 반환한다.
  이때 원본은 변경되지 않는다

  st.strip()
  문자열의 시작,끝 부분의 여백을 제거하여 반환한다.
  이때 원본은 변경되지 않는다

  
